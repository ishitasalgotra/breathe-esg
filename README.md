# Breathe ESG Data Ingestion Platform

Repository layout:

- `backend/` Django REST API and ingestion pipeline
- `frontend/` React/Vite analyst dashboard
- `docs/` architecture, decisions, tradeoffs, and source assumptions

Start with `docs/README.md` for setup and deployment steps.
